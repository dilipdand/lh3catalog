<p id="notice"><%= notice %></p>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="large-10 large-centered column">
			<%= @product.name %>
  </div>
  <div class="large-10 large-centered column">
	<%= @product.description %>
  </div>
<div class="large-10 large-centered column">
	<%= @product.all_categories %>
</div>
<div class="large-10 large-centered column">
	<%= @product.all_tags %>
</div>
<div class="large-10 large-centered column">
	<%= @product.website %>
</div>
<div class="large-10 large-centered column">
	<%= @product.logo %>
</div>
<div class="large-10 large-centered column">
	<%= @product.city + ", " + @product.state + ", " + @product.country %>
</div>
<div class="large-10 large-centered column">
	<%= @product.saas == true ? "SAAS" : "Not SAAS" %>
</div>
<div class="large-10 large-centered column">
	<%= @product.agency == true ? "Agency" : "Not an Agency" %>
</div>
<div class="large-10 large-centered column">
	<%= @product.api == true ? "Has API" : "Does Not Have API" %>
</div>
<div class="large-10 large-centered column">
	<%= @product.compensation %>
</div>
<div class="large-10 large-centered column">
	<%= @product.freemium == true ? "Freemium Model" : "No Freemium" %>
</div>
<div class="large-10 large-centered column">
	<%= "Per Campaign Price: " + (@product.per_campaign_price == nil ? "" : @product.per_campaign_price) %>
</div>
<div class="large-10 large-centered column">
	<%= "Base Price: " + (@product.base_price == nil ? "" : @product.base_price ) %>
</div>
<div class="large-10 large-centered column">
	<%= "Small Business Price: " + (@product.smb_price == nil ? "" : @product.smb_price)%>
</div>
<div class="large-10 large-centered column">
	<%= "Enterprise Price: " + (@product.ent_price == nil ? "" : @product.ent_price)%>
</div>
<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>
